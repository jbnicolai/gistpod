#!/usr/bin/env node

var CLI = require('../lib/cli')
  , Cache = require('../lib/cache')


Cache.readGistCache(function (err, gists) {
    if (err) { CLI.dieError(err) }
    else     { CLI.print(CLI.formatGistList(gists)) }
})